import React from 'react'

const Home = () => {
    return (
        <div>
            <h1>Home Page</h1>
        </div>
    )
}

export default Home

{/* <div>
{authenticated ? (
  <>
    <Home />
  </>
) : authenticated === null ? (
  <Spinner animation="grow" />
) : (
  <Login />
)}
</div> */}